naftiko: 1.0.0-alpha2
info:
  label: Hugging Face Inference Providers API — Chat Completions
  description: 'Hugging Face Inference Providers API — Chat Completions. 1 operations. Lead operation: Create Chat Completion.
    Self-contained Naftiko capability covering one Hugging Face business surface.'
  tags:
  - Hugging Face
  - Chat Completions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    HUGGING_FACE_API_KEY: HUGGING_FACE_API_KEY
capability:
  consumes:
  - type: http
    namespace: inference-providers-chat-completions
    baseUri: https://router.huggingface.co
    description: Hugging Face Inference Providers API — Chat Completions business capability. Self-contained, no shared references.
    resources:
    - name: v1-chat-completions
      path: /v1/chat/completions
      operations:
      - name: createchatcompletion
        method: POST
        description: Create Chat Completion
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.HUGGING_FACE_API_KEY}}'
  exposes:
  - type: rest
    namespace: inference-providers-chat-completions-rest
    port: 8080
    description: REST adapter for Hugging Face Inference Providers API — Chat Completions. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/v1/chat/completions
      name: v1-chat-completions
      description: REST surface for v1-chat-completions.
      operations:
      - method: POST
        name: createchatcompletion
        description: Create Chat Completion
        call: inference-providers-chat-completions.createchatcompletion
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: inference-providers-chat-completions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Hugging Face Inference Providers API — Chat Completions. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: create-chat-completion
      description: Create Chat Completion
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: inference-providers-chat-completions.createchatcompletion
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
